/* LegendaryAI backend configuration.
 * Create a Supabase project and replace the two placeholders below.
 * The anon key is safe for browser use when RLS is configured correctly.
 * NEVER put the service_role key or MoMo secretKey here.
 */
window.LEGENDARY_SUPABASE_CONFIG = {
  url: "https://YOUR_PROJECT_REF.supabase.co",
  anonKey: "YOUR_SUPABASE_ANON_KEY"
};
